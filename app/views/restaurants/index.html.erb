<h1 class="title">Index Restaurant</h1>

<ul class="card">
  <% @restaurants.each do |restaurant| %>
    <h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3>
    <p><%= restaurant.address %></p>
    <p><%= restaurant.phone_number %></p>
  <% end %>
</ul>